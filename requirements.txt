# Dépendances du projet AI-Image (covers de musique par style)
#
# IMPORTANT : à installer sur ton Mac Apple Silicon (M4 Pro).
# mflux fournit FLUX.1/FLUX.2/Z-Image en natif Metal (MLX) + l'entraînement LoRA.
# Il ne s'installe QUE sur macOS Apple Silicon (il dépend de `mlx`).

mflux>=0.18.0      # génération + entraînement LoRA natif Apple Silicon
#                  # ⚠️ FLUX.2 Klein (flux2-klein-*) nécessite une version RÉCENTE de
#                  #    mflux : « pip install -U mflux » si tu vises FLUX.2.
Pillow>=10.0.0     # préparation/redimensionnement des images
PyYAML>=6.0        # lecture des fichiers style.yaml

# Interface web locale (AI-Image Studio)
fastapi>=0.110     # serveur API
uvicorn>=0.27      # serveur ASGI
python-multipart>=0.0.9   # upload de fichiers depuis le navigateur

# Optionnel : auto-captioning par IA (scripts/caption.py).
#  • Backend LOCAL (--auto) : pip install mlx-vlm   (hors-ligne, Mac Apple Silicon)
# mlx-vlm>=0.1.0
#  • Backend GEMINI (--gemini) : AUCUNE dépendance (urllib de la stdlib).
#    Définis simplement GEMINI_API_KEY (clé gratuite sur aistudio.google.com/apikey).

